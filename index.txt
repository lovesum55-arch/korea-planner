<!DOCTYPE html>
<html lang="zh-TW">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>首爾自助旅行</title>
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Vue 3 -->
    <script src="https://unpkg.com/vue@3/dist/vue.global.js"></script>
    <!-- FontAwesome Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+TC:wght@300;400;500;700&display=swap" rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        khaki: {
                            50: '#FAF8F5',
                            100: '#F5F0E6',
                            200: '#E8DCC4',
                            300: '#DBC7A1',
                            400: '#CDB27E',
                            500: '#C09D5B',
                            600: '#A38245',
                            700: '#7A6134',
                            800: '#524123',
                            900: '#292011',
                        }
                    },
                    fontFamily: {
                        sans: ['Noto Sans TC', 'sans-serif'],
                    }
                }
            }
        }
    </script>
    <style>
        body { background-color: #FAF8F5; -webkit-tap-highlight-color: transparent; }
        .hide-scrollbar::-webkit-scrollbar { display: none; }
        .hide-scrollbar { -ms-overflow-style: none; scrollbar-width: none; }
        .glass-panel { background: rgba(255, 255, 255, 0.85); backdrop-filter: blur(10px); }
        .animate-fade-in { animation: fadeIn 0.3s ease-in-out; }
        @keyframes fadeIn { from { opacity: 0; transform: translateY(5px); } to { opacity: 1; transform: translateY(0); } }
    </style>
</head>
<body class="text-gray-800 antialiased">
    <div id="app" class="max-w-md mx-auto bg-khaki-50 min-h-screen relative shadow-xl overflow-hidden">
        
        <!-- 頁首 -->
        <div class="relative h-64 bg-khaki-600 rounded-b-3xl z-30 shadow-sm">
            <div class="absolute top-12 left-6 text-white text-shadow">
                <h1 class="text-3xl font-bold tracking-wider">首爾之旅</h1>
                <p class="text-sm mt-2 opacity-90">2026.04.05 - 04.08</p>
            </div>
            
            <!-- 浮動 Tab 按鈕 -->
            <div class="absolute bottom-6 right-6 flex space-x-3 z-40">
                <button v-for="tab in tabs" :key="tab.id" @click="currentTab = tab.id"
                        class="w-10 h-10 rounded-full flex items-center justify-center transition-all duration-300 shadow-md"
                        :class="currentTab === tab.id ? 'bg-khaki-800 text-white scale-110' : 'bg-white text-khaki-600 opacity-90'">
                    <i :class="tab.icon"></i>
                </button>
            </div>
        </div>

        <!-- 主內容區域 -->
        <div class="relative z-20 -mt-12 bg-khaki-50 min-h-screen rounded-t-3xl px-5 pt-14 pb-24">
            
            <!-- 1. 每日行程表 -->
            <div v-show="currentTab === 'itinerary'" class="animate-fade-in">
                <!-- 天氣資訊 -->
                <div class="glass-panel rounded-2xl p-4 mb-6 flex justify-between items-center shadow-sm border border-khaki-100">
                    <div>
                        <p class="text-xs text-khaki-600 font-medium">今日首爾天氣</p>
                        <p class="text-2xl font-bold text-gray-700 mt-1">15°C</p>
                    </div>
                    <div class="text-right flex items-center gap-3">
                        <div class="text-right">
                            <p class="text-xs text-gray-500">體感 14°C</p>
                            <p class="text-xs text-gray-500">晴時多雲</p>
                        </div>
                        <i class="fa-solid fa-cloud-sun text-3xl text-khaki-500"></i>
                    </div>
                </div>

                <!-- 日期橫向滑動選單 -->
                <div class="flex overflow-x-auto hide-scrollbar space-x-4 mb-6 pb-2">
                    <div v-for="day in days" :key="day.date" @click="selectedDay = day.id"
                         class="flex-shrink-0 flex flex-col items-center justify-center w-16 h-20 rounded-2xl cursor-pointer transition-colors"
                         :class="selectedDay === day.id ? 'bg-khaki-600 text-white shadow-md' : 'bg-white text-gray-500 border border-khaki-200'">
                        <span class="text-lg font-bold">{{ day.weekday }}</span>
                        <span class="text-xs mt-1">{{ day.date }}</span>
                    </div>
                </div>

                <!-- 行程卡片列表 -->
                <div class="space-y-4">
                    <div v-for="(item, index) in currentDayItinerary" :key="index">
                        
                        <!-- 交通時間 -->
                        <div v-if="index > 0" class="flex items-center justify-center py-2 text-khaki-400 text-xs">
                            <i class="fa-solid fa-ellipsis-vertical mr-2"></i>
                            <i :class="getTransportIcon(item.transportType)" class="mr-1"></i>
                            <span>{{ item.transportTime || '30分鐘' }}</span>
                        </div>

                        <!-- 航班特殊卡片 -->
                        <div v-if="item.type === 'flight'" class="bg-[#4A7C7A] text-white rounded-2xl p-4 shadow-md relative overflow-hidden group">
                            <button @click="deleteItineraryItem(selectedDay, index)" class="absolute top-4 right-4 text-white/60 hover:text-red-300 z-20 transition-colors">
                                <i class="fa-solid fa-trash-can"></i>
                            </button>
                            
                            <div class="flex justify-between items-center mb-4 pr-6">
                                <span class="text-xs opacity-80">{{ item.date }} | {{ item.flightNo }}</span>
                                <span class="text-xs bg-white/20 px-2 py-1 rounded-full">已確認</span>
                            </div>
                            <h3 class="text-xl font-bold mb-4">{{ item.name }}</h3>
                            <div class="flex justify-between items-center relative">
                                <div class="text-center z-10">
                                    <p class="text-2xl font-light">{{ item.depTime }}</p>
                                    <p class="text-xl font-bold mt-1">{{ item.depAirport }}</p>
                                </div>
                                <div class="flex-1 px-4 relative z-10">
                                    <div class="text-center text-xs opacity-80 mb-1">{{ item.duration }}</div>
                                    <div class="h-0.5 bg-white/30 w-full relative">
                                        <i class="fa-solid fa-plane absolute top-1/2 -translate-y-1/2 left-0 text-white"></i>
                                        <div class="w-2 h-2 rounded-full bg-white absolute top-1/2 -translate-y-1/2 right-0"></div>
                                    </div>
                                </div>
                                <div class="text-center z-10">
                                    <p class="text-2xl font-light">{{ item.arrTime }}</p>
                                    <p class="text-xl font-bold mt-1">{{ item.arrAirport }}</p>
                                </div>
                            </div>
                        </div>

                        <!-- 一般行程卡片 -->
                        <div v-else class="bg-white rounded-2xl p-4 shadow-sm border border-khaki-100 flex gap-4 relative group">
                            <button @click="deleteItineraryItem(selectedDay, index)" class="absolute top-4 right-4 text-gray-300 hover:text-red-500 z-20 transition-colors">
                                <i class="fa-solid fa-trash-can"></i>
                            </button>

                            <div class="w-12 h-12 rounded-full bg-khaki-100 flex items-center justify-center flex-shrink-0 text-khaki-600">
                                <i :class="getCategoryIcon(item.category)"></i>
                            </div>
                            <div class="flex-1 pr-6">
                                <div class="flex justify-between items-start">
                                    <h3 class="font-bold text-gray-800 text-lg">{{ item.name }}</h3>
                                </div>
                                <span class="text-xs text-khaki-500 font-medium block mt-1">{{ item.time }}</span>
                                <p v-if="item.notes" class="text-sm text-gray-500 mt-1">{{ item.notes }}</p>
                                
                                <div class="mt-3 flex gap-2">
                                    <a :href="'https://map.naver.com/v5/search/' + encodeURIComponent(item.name)" target="_blank" 
                                       class="text-xs bg-khaki-50 text-khaki-700 px-3 py-1.5 rounded-lg flex items-center gap-1 hover:bg-khaki-100">
                                        <i class="fa-solid fa-map-location-dot"></i> 導航
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    <div v-if="currentDayItinerary.length === 0" class="text-center py-10 text-gray-400">
                        目前沒有行程。
                    </div>
                </div>
            </div>

            <!-- 2. 資訊 -->
            <div v-show="currentTab === 'info'" class="space-y-6 animate-fade-in">
                <!-- 匯率換算 -->
                <div class="bg-white rounded-2xl p-5 shadow-sm border border-khaki-100">
                    <h3 class="font-bold text-gray-800 mb-4 flex items-center gap-2"><i class="fa-solid fa-money-bill-transfer text-khaki-500"></i> 匯率換算</h3>
                    <div class="flex items-center gap-4">
                        <div class="flex-1">
                            <label class="text-xs text-gray-500">韓元 (KRW)</label>
                            <input type="number" v-model="krwInput" class="w-full border-b-2 border-khaki-200 py-1 focus:outline-none focus:border-khaki-500 bg-transparent text-lg">
                        </div>
                        <i class="fa-solid fa-arrow-right-arrow-left text-khaki-300 mt-4"></i>
                        <div class="flex-1">
                            <label class="text-xs text-gray-500">港幣 (HKD)</label>
                            <div class="w-full border-b-2 border-transparent py-1 text-lg font-bold text-khaki-700">
                                ${{ (krwInput * exchangeRate).toFixed(2) }}
                            </div>
                        </div>
                    </div>
                    <p class="text-xs text-gray-400 mt-2 text-right">參考匯率: 1 KRW = {{ exchangeRate }} HKD</p>
                </div>

                <!-- 住宿資訊 -->
                <div class="bg-white rounded-2xl p-5 shadow-sm border border-khaki-100">
                    <div class="flex justify-between items-start mb-4">
                        <h3 class="font-bold text-gray-800 flex items-center gap-2"><i class="fa-solid fa-bed text-khaki-500"></i> 住宿資訊</h3>
                        <i class="fa-brands fa-airbnb text-red-500 text-xl"></i>
                    </div>
                    <h4 class="font-bold text-md mb-2">首爾站、淑大入口站步行3分鐘...</h4>
                    <div class="grid grid-cols-2 gap-4 text-sm mb-4">
                        <div>
                            <p class="text-gray-500 text-xs">入住</p>
                            <p class="font-medium">4月5日 15:00</p>
                        </div>
                        <div>
                            <p class="text-gray-500 text-xs">退房</p>
                            <p class="font-medium">4月8日 11:00</p>
                        </div>
                    </div>
                    <div class="space-y-2 text-sm">
                        <p class="flex items-start gap-2"><i class="fa-solid fa-location-dot mt-1 text-khaki-400"></i> <span>11-29 Hangang-daero 76-gil 101호, 首爾 04352</span></p>
                        <p class="flex items-center gap-2"><i class="fa-solid fa-phone text-khaki-400"></i> <span>+82 10-9941-2750 (Mini)</span></p>
                    </div>
                    <a href="https://map.naver.com/v5/search/11-29%20Hangang-daero%2076-gil" target="_blank" class="mt-4 block text-center bg-khaki-100 text-khaki-700 py-2 rounded-xl text-sm font-medium">開啟 Naver 導航</a>
                </div>
            </div>

            <!-- 3. 購物清單 -->
            <div v-show="currentTab === 'shopping'" class="animate-fade-in">
                <div class="grid grid-cols-2 gap-4">
                    <div v-for="(item, index) in shoppingList" :key="index" class="bg-white rounded-2xl p-3 shadow-sm border border-khaki-100 relative group">
                        <button @click="deleteShoppingItem(index)" class="absolute top-2 right-2 w-7 h-7 bg-white/80 rounded-full text-gray-400 hover:text-red-500 flex items-center justify-center z-10 shadow-sm transition-colors">
                            <i class="fa-solid fa-trash-can text-xs"></i>
                        </button>

                        <div class="h-24 bg-khaki-100 rounded-xl mb-3 flex items-center justify-center overflow-hidden">
                            <i v-if="!item.img" class="fa-solid fa-image text-khaki-300 text-2xl"></i>
                            <img v-else :src="item.img" class="w-full h-full object-cover">
                        </div>
                        <h4 class="font-medium text-sm text-gray-800">{{ item.name }}</h4>
                        <div class="mt-2 flex justify-between items-center">
                            <span class="text-xs text-gray-500">{{ item.price }} KRW</span>
                            <input type="checkbox" class="accent-khaki-600 w-4 h-4 rounded">
                        </div>
                    </div>
                </div>
                
                <div v-if="shoppingList.length === 0" class="text-center py-10 text-gray-400">
                    購物清單為空。
                </div>
            </div>

            <!-- 4. 花費 -->
            <div v-show="currentTab === 'expenses'" class="animate-fade-in">
                <div class="bg-khaki-700 text-white rounded-2xl p-6 mb-6 shadow-md text-center">
                    <p class="text-sm opacity-80 mb-1">總花費 (KRW)</p>
                    <h2 class="text-3xl font-bold">₩ {{ totalExpenses.toLocaleString() }}</h2>
                    <p class="text-xs mt-2 opacity-70">約 HKD ${{ (totalExpenses * exchangeRate).toFixed(0) }}</p>
                </div>

                <div class="space-y-3">
                    <div v-for="(exp, index) in expenses" :key="index" class="bg-white p-4 rounded-2xl shadow-sm border border-khaki-100 flex items-center justify-between relative">
                        <div class="flex items-center gap-3">
                            <div class="w-10 h-10 rounded-full bg-khaki-50 flex items-center justify-center text-khaki-600">
                                <i :class="getCategoryIcon(exp.category)"></i>
                            </div>
                            <div>
                                <h4 class="font-bold text-sm text-gray-800">{{ exp.name }}</h4>
                                <p class="text-xs text-gray-500">{{ exp.date }} · {{ exp.method }}</p>
                            </div>
                        </div>
                        <div class="flex items-center gap-3">
                            <div class="text-right font-bold text-khaki-700">
                                ₩ {{ exp.amount.toLocaleString() }}
                            </div>
                            <button @click="deleteExpense(index)" class="text-gray-300 hover:text-red-500 transition-colors">
                                <i class="fa-solid fa-trash-can"></i>
                            </button>
                        </div>
                    </div>
                </div>
                
                <div v-if="expenses.length === 0" class="text-center py-10 text-gray-400">
                    目前沒有花費紀錄。
                </div>
            </div>

        </div>

        <!-- 共用的浮動畫筆按鈕 (在行程、購物、花費分頁皆會顯示，點擊開啟新增花費) -->
        <button v-show="['itinerary', 'shopping', 'expenses'].includes(currentTab)" 
                @click="showAddExpenseModal = true" 
                class="fixed bottom-6 right-6 w-14 h-14 bg-khaki-700 text-white rounded-full shadow-lg flex items-center justify-center text-xl z-40 hover:bg-khaki-800 transition-colors">
            <i class="fa-solid fa-pen"></i>
        </button>

        <!-- 新增花費彈出視窗 (Modal) -->
        <div v-if="showAddExpenseModal" class="fixed inset-0 z-50 flex items-center justify-center p-4 bg-black bg-opacity-50 transition-opacity">
            <div class="bg-white rounded-3xl w-full max-w-sm p-6 shadow-xl">
                <h3 class="text-lg font-bold mb-4 text-gray-800 flex items-center gap-2">
                    <i class="fa-solid fa-pen-to-square text-khaki-500"></i> 新增花費
                </h3>
                
                <div class="space-y-4">
                    <div>
                        <label class="block text-xs text-gray-500 mb-1">類別</label>
                        <select v-model="newExpense.category" class="w-full border border-gray-200 rounded-lg p-2 text-sm focus:outline-none focus:border-khaki-500 bg-white">
                            <option value="food">飲食</option>
                            <option value="transport">交通</option>
                            <option value="shopping">購物</option>
                            <option value="attraction">門票</option>
                            <option value="hotel">住宿</option>
                            <option value="other">其他</option>
                        </select>
                    </div>
                    
                    <div>
                        <label class="block text-xs text-gray-500 mb-1">名稱</label>
                        <input type="text" v-model="newExpense.name" placeholder="例如：晚餐烤肉" class="w-full border border-gray-200 rounded-lg p-2 text-sm focus:outline-none focus:border-khaki-500">
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="block text-xs text-gray-500 mb-1">金額 (KRW)</label>
                            <input type="number" v-model="newExpense.amount" placeholder="0" class="w-full border border-gray-200 rounded-lg p-2 text-sm focus:outline-none focus:border-khaki-500">
                        </div>
                        <div>
                            <label class="block text-xs text-gray-500 mb-1">付款方式</label>
                            <select v-model="newExpense.method" class="w-full border border-gray-200 rounded-lg p-2 text-sm focus:outline-none focus:border-khaki-500 bg-white">
                                <option value="現金">現金</option>
                                <option value="信用卡">信用卡</option>
                                <option value="T-money">T-money</option>
                            </select>
                        </div>
                    </div>
                </div>

                <div class="mt-6 flex gap-3">
                    <button @click="showAddExpenseModal = false" class="flex-1 py-2 rounded-xl text-sm font-medium text-gray-500 bg-gray-100 hover:bg-gray-200 transition-colors">取消</button>
                    <button @click="addExpense" class="flex-1 py-2 rounded-xl text-sm font-medium text-white bg-khaki-600 hover:bg-khaki-700 transition-colors">儲存</button>
                </div>
            </div>
        </div>

    </div>

    <script>
        const { createApp, ref, computed } = Vue;

        createApp({
            setup() {
                const currentTab = ref('itinerary');
                const selectedDay = ref('day1');
                const krwInput = ref(10000);
                const exchangeRate = 0.0058; // 1 KRW = 0.0058 HKD
                
                // Modal 狀態
                const showAddExpenseModal = ref(false);
                const newExpense = ref({
                    category: 'food',
                    name: '',
                    amount: '',
                    method: '現金'
                });

                const tabs = [
                    { id: 'itinerary', icon: 'fa-regular fa-calendar-check' },
                    { id: 'info', icon: 'fa-solid fa-circle-info' },
                    { id: 'shopping', icon: 'fa-solid fa-bag-shopping' },
                    { id: 'expenses', icon: 'fa-solid fa-wallet' }
                ];

                const days = [
                    { id: 'day1', weekday: '六', date: '4/5' },
                    { id: 'day2', weekday: '日', date: '4/6' },
                    { id: 'day3', weekday: '一', date: '4/7' },
                    { id: 'day4', weekday: '二', date: '4/8' }
                ];

                const itinerary = ref({
                    day1: [
                        { type: 'flight', name: '香港飛往首爾', date: '4月5日', flightNo: 'CX410', depTime: '09:20', depAirport: 'HKG', arrTime: '14:10', arrAirport: 'ICN', duration: '3h 50m' },
                        { type: 'normal', category: 'transport', name: '機場快線 AREX 到首爾站', time: '15:00 - 15:45', transportType: 'train', transportTime: '45分鐘' },
                        { type: 'normal', category: 'hotel', name: 'Airbnb Check-in', time: '16:00', notes: '首爾站附近', transportType: 'walk', transportTime: '15分鐘' },
                        { type: 'normal', category: 'food', name: '晚餐：明洞餃子', time: '18:00', notes: '米其林必比登推薦', transportType: 'subway', transportTime: '20分鐘' }
                    ],
                    day2: [
                        { type: 'normal', category: 'attraction', name: '景福宮 (穿韓服)', time: '10:00 - 13:00', notes: '週二休館，記得提早租韓服' },
                        { type: 'normal', category: 'food', name: '午餐：土俗村蔘雞湯', time: '13:30', transportType: 'walk', transportTime: '10分鐘' },
                        { type: 'normal', category: 'shopping', name: '北村韓屋村 & 三清洞', time: '15:00 - 18:00', transportType: 'walk', transportTime: '15分鐘' }
                    ],
                    day3: [],
                    day4: []
                });

                const shoppingList = ref([
                    { name: 'Olive Young 面膜', price: '35,000', img: '' },
                    { name: 'Gentle Monster 墨鏡', price: '280,000', img: '' },
                    { name: 'Tamburins 護手霜', price: '32,000', img: '' }
                ]);

                const expenses = ref([
                    { category: 'food', name: '明洞餃子', amount: 22000, date: '4/5', method: '現金' },
                    { category: 'transport', name: 'AREX 車票', amount: 19000, date: '4/5', method: '信用卡' },
                    { category: 'shopping', name: 'Olive Young', amount: 45000, date: '4/6', method: '信用卡' }
                ]);

                const currentDayItinerary = computed(() => {
                    return itinerary.value[selectedDay.value] || [];
                });

                const totalExpenses = computed(() => {
                    return expenses.value.reduce((total, exp) => total + exp.amount, 0);
                });

                const getCategoryIcon = (category) => {
                    const icons = {
                        food: 'fa-solid fa-utensils',
                        transport: 'fa-solid fa-train-subway',
                        shopping: 'fa-solid fa-bag-shopping',
                        attraction: 'fa-solid fa-camera',
                        hotel: 'fa-solid fa-bed',
                        other: 'fa-solid fa-star'
                    };
                    return icons[category] || icons.other;
                };

                const getTransportIcon = (type) => {
                    const icons = {
                        train: 'fa-solid fa-train',
                        subway: 'fa-solid fa-train-subway',
                        bus: 'fa-solid fa-bus',
                        walk: 'fa-solid fa-person-walking',
                        taxi: 'fa-solid fa-taxi'
                    };
                    return icons[type] || 'fa-solid fa-arrow-right';
                };

                const deleteItineraryItem = (dayId, index) => {
                    if(confirm('確定要刪除這個行程嗎？')) {
                        itinerary.value[dayId].splice(index, 1);
                    }
                };

                const deleteShoppingItem = (index) => {
                    if(confirm('確定要刪除這個購物項目嗎？')) {
                        shoppingList.value.splice(index, 1);
                    }
                };

                const deleteExpense = (index) => {
                    if(confirm('確定要刪除這筆花費嗎？')) {
                        expenses.value.splice(index, 1);
                    }
                };

                const addExpense = () => {
                    if (!newExpense.value.name || !newExpense.value.amount) {
                        alert('請輸入名稱與金額');
                        return;
                    }
                    
                    // 取得今日日期 (格式 MM/DD)
                    const today = new Date();
                    const dateStr = `${today.getMonth() + 1}/${today.getDate()}`;

                    expenses.value.unshift({
                        category: newExpense.value.category,
                        name: newExpense.value.name,
                        amount: Number(newExpense.value.amount),
                        date: dateStr,
                        method: newExpense.value.method
                    });

                    // 重置表單並關閉 Modal
                    newExpense.value = {
                        category: 'food',
                        name: '',
                        amount: '',
                        method: '現金'
                    };
                    showAddExpenseModal.value = false;
                    
                    // 自動切換到花費分頁查看
                    currentTab.value = 'expenses';
                };

                return {
                    currentTab, tabs, days, selectedDay, currentDayItinerary,
                    krwInput, exchangeRate, shoppingList, expenses, totalExpenses,
                    getCategoryIcon, getTransportIcon,
                    deleteItineraryItem, deleteShoppingItem, deleteExpense,
                    showAddExpenseModal, newExpense, addExpense
                };
            }
        }).mount('#app');
    </script>
</body>
</html>